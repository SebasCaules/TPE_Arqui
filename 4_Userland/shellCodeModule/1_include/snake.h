#ifndef TPE_ARQUI_SNAKE_H
#define TPE_ARQUI_SNAKE_H

#include <stdint.h>

/**
 * @brief Initializes and starts the Snake game.
 * @return An integer status code upon the game's completion (e.g., 0 for successful execution).
 *
 * This function sets up the initial game parameters, presents the start view,
 * and calls the main game loop to begin gameplay. 
 */
void snake();

#endif